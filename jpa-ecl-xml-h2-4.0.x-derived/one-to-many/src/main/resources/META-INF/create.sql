CREATE TABLE TEMPLATECLASS (ID INTEGER NOT NULL, NAME VARCHAR, VERSION VARCHAR, PRIMARY KEY (ID))
CREATE TABLE TEMPLATESUBCLASS (ID INTEGER NOT NULL, SERIAL VARCHAR, TEMPLATECLASS_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE TEMPLATESUBCLASS ADD CONSTRAINT FK_TEMPLATESUBCLASS_TEMPLATECLASS_ID FOREIGN KEY (TEMPLATECLASS_ID) REFERENCES TEMPLATECLASS (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE (SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
